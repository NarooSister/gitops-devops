apiVersion: v1
kind: Service
metadata:
  name: my-api
  namespace: my-api
spec:
  type: NodePort
  selector:
    app: my-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30080